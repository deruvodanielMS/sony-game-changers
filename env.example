DATABASE_URL="file:./prisma/dev.db"

GOALS_SOURCE=mock
# Options: mock (demo data), prisma (local database), vendor (external API)
# mock: Uses static demo data (no database required)
# prisma: Uses local SQLite database
# vendor: Uses external vendor API

#VENDOR_API_URL=
#VENDOR_API_TOKEN=


